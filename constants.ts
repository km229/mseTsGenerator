
export const FAMIX_PREFIX = "Famix-TypeScript-Entities"
export const enum MSE_FORMAT { JSON, MSE }
export const json_const = {
    open_token: "{",
    close_token: "}"
}
export const mse_const = {
    open_token: "(",
    close_token: ")"
}
